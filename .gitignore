# Cargo build dirs and `pds doc` output (out = target/doc), at any depth.
target/
.DS_Store
.idea/
.playwright-mcp/
node_modules/
# Graphviz source clone — algorithm/oracle reference for pseudoscript-dot, not vendored.
reference/
