// LANG.md §5.1 (ADR-041): `Name(args)` must name a callable of the
// enclosing node. `missing` is not declared on `C`.

//! example

public system S;

public container C for S {
  run(): void { missing() }
}
