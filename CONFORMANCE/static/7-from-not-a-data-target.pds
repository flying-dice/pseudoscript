// LANG.md §7.2 (ADR-035): a `from` target MAY be any type except a node; a node
// target is rejected.

//! example

public data Box { id: number }

public system Banking;

public container Mainframe for Banking {
  Make(): Box {
    b = Banking from { }
    return Box from { }
  }
}
