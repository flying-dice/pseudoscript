// LANG.md §7.2 (ADR-020): a `from` target must be a `data` record or union
// variant; a primitive target is rejected.

//! example

public system Banking;

public container Mainframe for Banking {
  Label(id: number): string {
    return string from { id }
  }
}
