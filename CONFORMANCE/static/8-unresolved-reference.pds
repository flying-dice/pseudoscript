// LANG.md §7/§8 (ADR-022): a bare reference must resolve to a param, binding,
// node, or variant. `ghost` is undeclared.

//! example

public data Item { id: number }

public system S;

public container C for S {
  get(): Item { return ghost }
}
