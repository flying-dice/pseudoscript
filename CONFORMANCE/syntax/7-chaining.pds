// LANG.md §7 (ADR-007): `.` access and calls chain freely — field-off-call,
// field-of-field, and call chains all parse.

public system Banking;

data Info { owner: string }

public container Mainframe for Banking {
  Run(): void {
    a = string from Store::Repo.fetch(1).value.owner
    b = string from self.Pick().first().tag
  }
}
